{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ recette.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ recette.content }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ recette.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_recette_index') }}">back to list</a>

    <a href="{{ path('admin_recette_edit', {'id': recette.id}) }}">edit</a>

    {{ include('admin_recette/_delete_form.html.twig') }}
{% endblock %}
