{% extends 'base.html.twig' %}

{% block title %}Culture{% endblock %}

{% block body %}
    <h1>Culture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ culture.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ culture.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ culture.content }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ culture.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_culture_index') }}">back to list</a>

    <a href="{{ path('admin_culture_edit', {'id': culture.id}) }}">edit</a>

    {{ include('admin_culture/_delete_form.html.twig') }}
{% endblock %}
