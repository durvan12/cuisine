{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<header id="images" class="text-center"></header>

<main class="mb-3 mt-4">
  <div class="row row-cols-1 row-cols-md-2 g-4">
    {# boucler pour en afficher le nombre voulu (nécessite une méthode pour le faire) #}
    <div class="col">
      <div class="card">
        <img src="{{asset('img/couscoussi.jpg')}}" class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title">Couscous Au poisson</h5>
          <p class="card-text">couscous avec la dorade</p>
          <a href="#" class="card-link">Recette Complete</a>
        </div>
      </div>
    </div>
    {# fin de la boucle #}
  </div>
</main>
{% endblock %}